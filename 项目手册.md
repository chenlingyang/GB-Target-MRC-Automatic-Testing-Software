# 图像采集卡上位机软件 - 项目手册

**版本**: 1.0  
**最后更新**: 2026-02-17  
**开发环境**: Visual Studio 2022, .NET 8.0, C# WPF

---

## 目录

1. [项目概述](#项目概述)
2. [项目结构](#项目结构)
3. [核心模块说明](#核心模块说明)
4. [配置文件说明](#配置文件说明)
5. [如何更换图像采集卡](#如何更换图像采集卡)
6. [功能模块详解](#功能模块详解)
7. [常见问题与解决方案](#常见问题与解决方案)
8. [开发指南](#开发指南)

---

## 项目概述

### 项目名称
图像采集卡上位机软件 (Image Capture Card Host Software)

### 主要功能
- ✅ 实时图像采集（支持DirectShow接口）
- ✅ 图像显示与缩放（支持鼠标滚轮缩放）
- ✅ 图像平移（支持鼠标拖拽）
- ✅ 图像灰度化处理
- ✅ 图像保存（支持多种格式）

### 技术栈
- **框架**: .NET 8.0 WPF
- **图像处理**: EmguCV 4.8.1 (OpenCV的C#封装)
- **UI框架**: WPF (Windows Presentation Foundation)
- **配置管理**: JSON配置文件

---

## 项目结构

```
ImageCaptureApp/
├── Config/                          # 配置文件夹
│   ├── CaptureDeviceConfig.json    # 采集卡配置文件（可修改）
│   └── CaptureDeviceConfig.cs      # 配置管理类
│
├── Controls/                        # 自定义控件
│   ├── ImageDisplayControl.xaml    # 图像显示控件UI
│   └── ImageDisplayControl.xaml.cs # 图像显示控件逻辑
│
├── Modules/                         # 核心功能模块
│   ├── ImageCaptureModule.cs       # 图像采集模块
│   ├── ImageProcessingModule.cs    # 图像处理模块
│   └── ImageStorageModule.cs      # 图像存储模块
│
├── Properties/                      # 项目属性
│   └── AssemblyInfo.cs            # 程序集信息
│
├── App.xaml                         # 应用程序入口UI
├── App.xaml.cs                     # 应用程序入口逻辑
├── MainWindow.xaml                 # 主窗口UI
├── MainWindow.xaml.cs              # 主窗口业务逻辑
├── app.manifest                    # 应用程序清单
└── ImageCaptureApp.csproj          # 项目文件
```

---

## 核心模块说明

### 1. ImageCaptureModule（图像采集模块）

**文件位置**: `Modules/ImageCaptureModule.cs`

**功能**:
- 管理图像采集卡的连接和初始化
- 实时采集图像数据
- 控制采集的开始和停止
- 配置采集参数（分辨率、帧率）

**关键方法**:
```csharp
bool Initialize(int deviceIndex = 0)      // 初始化采集卡
bool StartCapture()                       // 开始采集
void StopCapture()                        // 停止采集
void SetResolution(int width, int height) // 设置分辨率
void SetFrameRate(int fps)                // 设置帧率
```

**事件**:
- `FrameReceived`: 新帧到达事件
- `CaptureStateChanged`: 采集状态变化事件

**使用位置**: `MainWindow.xaml.cs` 中初始化和管理采集

---

### 2. ImageProcessingModule（图像处理模块）

**文件位置**: `Modules/ImageProcessingModule.cs`

**功能**:
- 图像灰度化转换
- 图像缩放处理
- 图像裁剪
- 亮度/对比度调整

**关键方法**:
```csharp
Mat ConvertToGrayscale(Mat source)              // 转换为灰度图
Mat ResizeImage(Mat source, double scale)        // 按比例缩放
Mat ResizeImage(Mat source, int width, int height) // 指定尺寸缩放
Mat CropImage(Mat source, Rectangle region)      // 裁剪图像
```

**使用位置**: 
- `MainWindow.xaml.cs`: 灰度化处理
- `ImageDisplayControl.xaml.cs`: 图像缩放（通过RenderTransform实现）

---

### 3. ImageStorageModule（图像存储模块）

**文件位置**: `Modules/ImageStorageModule.cs`

**功能**:
- 保存图像到文件
- 支持多种图像格式（PNG、JPEG、BMP、TIFF）
- 文件保存对话框
- 批量保存功能

**关键方法**:
```csharp
bool SaveImage(Mat image, string filePath, ImageFormat format)
bool SaveImageWithDialog(Mat image, string defaultFileName)
int SaveImages(List<Mat> images, string directory, ImageFormat format)
```

**使用位置**: `MainWindow.xaml.cs` 中的保存功能

---

### 4. ImageDisplayControl（图像显示控件）

**文件位置**: `Controls/ImageDisplayControl.xaml` 和 `.xaml.cs`

**功能**:
- 显示图像
- 支持缩放（工具栏按钮、快捷键、鼠标滚轮）
- 支持平移（鼠标拖拽）
- 高质量图像渲染

**关键属性**:
- `ZoomScale`: 当前缩放比例

**关键方法**:
```csharp
void SetImage(Mat? image)    // 设置显示的图像
void ZoomIn()                // 放大
void ZoomOut()               // 缩小
void ResetView()             // 重置视图
```

**使用位置**: `MainWindow.xaml` 中作为图像显示区域

---

### 5. MainWindow（主窗口）

**文件位置**: `MainWindow.xaml` 和 `MainWindow.xaml.cs`

**功能**:
- 整合所有功能模块
- 管理用户界面
- 处理用户交互
- 加载和保存配置

**关键组件**:
- 菜单栏：文件、视图、处理、帮助
- 工具栏：常用功能快捷按钮
- 图像显示区域：`ImageDisplayControl`
- 状态栏：显示分辨率、帧率、缩放比例等信息

---

## 配置文件说明

### 配置文件位置
`ImageCaptureApp/Config/CaptureDeviceConfig.json`

### 配置文件结构

```json
{
  "DeviceSettings": {
    "DeviceIndex": 0,              // 设备索引（0=第一个设备）
    "DeviceName": "默认采集卡",    // 设备名称（用于显示）
    "API": "DirectShow",          // API类型
    "Resolution": {
      "Width": 640,                // 宽度
      "Height": 480                // 高度
    },
    "FrameRate": 30,               // 帧率
    "AutoStart": false             // 是否自动启动采集
  },
  "DisplaySettings": {
    "DefaultZoom": 1.0,           // 默认缩放比例
    "MinZoom": 0.1,               // 最小缩放
    "MaxZoom": 10.0               // 最大缩放
  },
  "StorageSettings": {
    "DefaultFormat": "PNG",       // 默认保存格式
    "DefaultSavePath": "",        // 默认保存路径（空=系统默认）
    "AutoSave": false             // 是否自动保存
  }
}
```

### 配置管理类

**文件位置**: `Config/CaptureDeviceConfig.cs`

**功能**:
- 加载配置文件
- 保存配置修改
- 提供类型安全的配置访问

**使用方法**:
```csharp
// 加载配置
var config = CaptureDeviceConfig.Load();

// 读取配置
int deviceIndex = config.DeviceSettings.DeviceIndex;
int width = config.DeviceSettings.Resolution.Width;

// 修改配置
config.DeviceSettings.DeviceIndex = 1;
config.Save();
```

---

## 如何更换图像采集卡

### 方法一：修改配置文件（推荐）

1. **找到配置文件**
   - 编译后的程序：`bin\Debug\net8.0-windows\Config\CaptureDeviceConfig.json`
   - 源代码：`ImageCaptureApp\Config\CaptureDeviceConfig.json`

2. **修改设备索引**
   ```json
   {
     "DeviceSettings": {
       "DeviceIndex": 1,  // 改为1表示第二个设备，2表示第三个，以此类推
       "DeviceName": "我的采集卡型号",
       ...
     }
   }
   ```

3. **修改分辨率**（如果需要）
   ```json
   "Resolution": {
     "Width": 1920,   // 根据采集卡支持的分辨率修改
     "Height": 1080
   }
   ```

4. **保存配置文件并重新运行程序**

### 方法二：修改代码

**文件**: `MainWindow.xaml.cs`

**位置**: `InitializeCaptureModule()` 方法

```csharp
// 修改这一行
if (_captureModule.Initialize(0))  // 将0改为你的设备索引
```

### 方法三：添加设备选择功能（扩展）

如果需要运行时切换设备，可以：

1. **添加设备列表获取功能**（在 `ImageCaptureModule.cs` 中）:
```csharp
public static List<string> GetAvailableDevices()
{
    var devices = new List<string>();
    for (int i = 0; i < 10; i++) // 检查前10个设备
    {
        using (var cap = new VideoCapture(i, VideoCapture.API.DShow))
        {
            if (cap.IsOpened)
            {
                devices.Add($"设备 {i}");
            }
        }
    }
    return devices;
}
```

2. **在主窗口添加设备选择下拉框**

---

## 功能模块详解

### 图像采集流程

```
1. 程序启动
   ↓
2. MainWindow 构造函数
   ↓
3. 加载配置文件 (CaptureDeviceConfig.Load())
   ↓
4. 初始化 ImageCaptureModule
   ↓
5. 调用 Initialize(deviceIndex)
   ↓
6. 创建 VideoCapture 对象
   ↓
7. 设置分辨率和帧率
   ↓
8. 如果 AutoStart=true，自动开始采集
   ↓
9. 采集循环（后台线程）
   ↓
10. 每帧触发 FrameReceived 事件
   ↓
11. MainWindow 接收事件，更新显示
```

### 图像显示流程

```
1. Mat 图像数据到达
   ↓
2. 根据灰度化模式处理图像
   ↓
3. ConvertMatToBitmapSource() 转换
   ↓
4. 创建 BitmapSource
   ↓
5. 设置到 ImageDisplayControl
   ↓
6. 应用缩放变换（RenderTransform）
   ↓
7. 显示在界面上
```

### 图像保存流程

```
1. 用户点击"保存"按钮
   ↓
2. 获取当前显示的图像（Mat）
   ↓
3. 显示保存对话框（SaveFileDialog）
   ↓
4. 用户选择路径和格式
   ↓
5. ImageStorageModule.SaveImage()
   ↓
6. CvInvoke.Imwrite() 写入文件
   ↓
7. 显示保存成功消息
```

---

## 常见问题与解决方案

### Q1: 提示"未检测到采集卡设备"

**可能原因**:
1. 设备未连接
2. 设备驱动未安装
3. 设备索引不正确
4. 设备被其他程序占用

**解决方案**:
1. 检查设备连接和电源
2. 在设备管理器中确认设备正常
3. 修改配置文件中的 `DeviceIndex`，尝试不同的索引值（0, 1, 2...）
4. 关闭其他可能占用设备的程序

### Q2: 图像显示卡顿

**可能原因**:
1. 分辨率过高
2. 帧率设置过高
3. 计算机性能不足

**解决方案**:
1. 在配置文件中降低分辨率
2. 降低帧率设置
3. 关闭其他占用资源的程序

### Q3: 保存图像失败

**可能原因**:
1. 路径无写入权限
2. 磁盘空间不足
3. 文件被占用

**解决方案**:
1. 选择有写入权限的目录
2. 检查磁盘空间
3. 确保文件未被其他程序打开

### Q4: 编译错误：找不到 EmguCV

**解决方案**:
1. 在 Visual Studio 中：工具 → NuGet包管理器 → 程序包管理器控制台
2. 运行：`Update-Package -reinstall`
3. 或手动安装：`Install-Package Emgu.CV`

---

## 开发指南

### 添加新功能

#### 1. 添加新的图像处理功能

**位置**: `Modules/ImageProcessingModule.cs`

**示例**（添加边缘检测）:
```csharp
public static Mat DetectEdges(Mat source)
{
    if (source == null || source.IsEmpty)
        return new Mat();
    
    Mat gray = new Mat();
    Mat edges = new Mat();
    
    // 转换为灰度
    CvInvoke.CvtColor(source, gray, ColorConversion.Bgr2Gray);
    
    // Canny边缘检测
    CvInvoke.Canny(gray, edges, 100, 200);
    
    gray.Dispose();
    return edges;
}
```

**在主窗口中使用**:
```csharp
// 在 MainWindow.xaml.cs 中添加按钮事件
private void BtnEdgeDetection_Click(object sender, RoutedEventArgs e)
{
    if (_originalImage != null)
    {
        Mat processed = ImageProcessingModule.DetectEdges(_originalImage);
        _currentDisplayImage?.Dispose();
        _currentDisplayImage = processed;
        ImageDisplay.SetImage(_currentDisplayImage);
    }
}
```

#### 2. 添加新的配置项

**步骤**:
1. 在 `CaptureDeviceConfig.json` 中添加新字段
2. 在 `CaptureDeviceConfig.cs` 中添加对应的属性类
3. 在代码中读取和使用配置

**示例**:
```json
// CaptureDeviceConfig.json
{
  "DeviceSettings": {
    ...
    "Brightness": 0,      // 新增：亮度调整
    "Contrast": 1.0       // 新增：对比度调整
  }
}
```

```csharp
// CaptureDeviceConfig.cs
public class DeviceSettings
{
    ...
    [JsonPropertyName("Brightness")]
    public int Brightness { get; set; } = 0;
    
    [JsonPropertyName("Contrast")]
    public double Contrast { get; set; } = 1.0;
}
```

#### 3. 修改UI布局

**文件**: `MainWindow.xaml`

**示例**（添加新按钮）:
```xml
<ToolBar Grid.Row="1">
    ...
    <Separator/>
    <Button Content="边缘检测" Click="BtnEdgeDetection_Click" Padding="10,5"/>
</ToolBar>
```

### 调试技巧

1. **使用断点**: 在关键位置设置断点，查看变量值
2. **输出窗口**: 使用 `System.Diagnostics.Debug.WriteLine()` 输出调试信息
3. **异常处理**: 所有关键操作都有 try-catch，查看异常信息
4. **事件查看器**: 查看 Windows 事件查看器中的应用程序日志

### 性能优化建议

1. **图像处理**: 在后台线程进行，避免阻塞UI
2. **内存管理**: 及时释放 Mat 对象，使用 using 语句
3. **图像显示**: 使用 BitmapSource.Freeze() 提升性能
4. **采集频率**: 根据需求调整帧率，避免不必要的处理

---

## 文件修改指南

### 修改采集卡设置
- **配置文件**: `Config/CaptureDeviceConfig.json`
- **代码位置**: `MainWindow.xaml.cs` → `InitializeCaptureModule()`

### 修改图像处理算法
- **文件**: `Modules/ImageProcessingModule.cs`
- **添加新方法**: 在类中添加静态方法

### 修改UI界面
- **文件**: `MainWindow.xaml`
- **布局**: 修改 Grid 行定义和控件位置
- **样式**: 添加样式资源

### 修改保存格式
- **文件**: `Modules/ImageStorageModule.cs`
- **方法**: `SaveImage()` 和 `GetSaveParameters()`

### 修改快捷键
- **文件**: `MainWindow.xaml` → `Window.InputBindings`
- **文件**: `MainWindow.xaml.cs` → `Window_KeyDown()`

---

## 版本历史

### v1.0 (2026-02-17)
- ✅ 初始版本发布
- ✅ 实现图像采集功能
- ✅ 实现图像缩放和平移
- ✅ 实现图像灰度化
- ✅ 实现图像保存功能
- ✅ 添加配置文件支持
- ✅ 完善项目文档

---

## 联系与支持

如有问题或建议，请：
1. 查看本文档的常见问题部分
2. 检查代码注释
3. 查看 Visual Studio 输出窗口的错误信息

---

**文档结束**
